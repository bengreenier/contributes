{
  "name": "@overlayed-app/contributes",
  "version": "1.3.0",
  "description": "parse JSONSchema contribution entries, and validate data against them",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "test": "jest",
    "publish": "npm publish --access public",
    "publish-coverage": "cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "overlayed"
  ],
  "author": "Ben Greenier",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "@types/node": "^10.12.2",
    "coveralls": "^3.0.2",
    "jest": "^23.6.0",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "ajv": "^6.5.5"
  }
}
